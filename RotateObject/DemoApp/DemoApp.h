#pragma once
#include "../RotateObject/GameApp.h"
#include "resource.h"

class DemoApp : public GameApp
{
private:
public:
	DemoApp(HINSTANCE hInstance, UINT width, UINT height);
	~DemoApp();

	//virtual void Update();
};
